# Meme Sound Files

Add your meme audio files in this folder with these exact names:

- `7_crore_meme_sound_kbc.mp3` (played when team scores points)
- `faaa.mp3` (played when team scores 0)

The app will automatically play these sounds on result reveal.

If a file is missing, the app falls back to a built-in synthesized sound.
